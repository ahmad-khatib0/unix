sudo apt update
sudo apt dist-upgrade

# auto update dependincies
yes | sudo apt install unattended-upgrades

# Occasionally, you’ll also need to remove some old packages that are no longer needed
# sudo apt auto-remove

